<h1><%=@doctor.name %></h1>

<h2>Appointments:</h2>
<% @doctor.appointments.each do |appt| %>
    <%= appt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %><br><%= link_to appt.patient.name, patient_path(appt.patient)%>
    <%end%>
