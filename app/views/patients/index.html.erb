<h2>All Patients</h2>
<ul>
<% @patients.each do |patient| %>
    <li><strong>Name:</strong> <%= link_to patient.name, patient_path(patient) %>
    <strong>Number of Appointments:</strong> <%= patient.appointments.count %></li>
    <br>
<%end%>
</ul>